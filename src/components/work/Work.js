import React from 'react';
import { Element } from 'react-scroll';
import Carousel from './Carousel';

const Work = () => (
  <section id="work">
    <Element name="work" />
    <h1>Work</h1>
    <Carousel />
  </section>
);

export default Work;
